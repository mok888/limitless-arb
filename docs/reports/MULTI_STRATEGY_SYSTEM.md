# 多策略多账户交易执行系统

## 系统概述

基于您的需求，我设计并实现了一个灵活的多策略多账户交易执行系统，支持：

### 🎯 核心功能
- **策略A**: 新市场Split策略 - 实时监控市场，发现新市场时将100u split成yes和no
- **策略B**: LP做市策略 - 选择合适市场购买50份额，然后做LP，设置止盈率
- **多账户管理**: 支持账户1执行策略A+B，账户2执行策略A，账户3执行策略B
- **命令行界面**: 灵活的交易执行策略配置和监控

## 🏗️ 系统架构

```
多策略交易系统
├── 策略管理器 (StrategyManager)
│   ├── NewMarketSplitStrategy - 新市场Split策略
│   └── LPMakingStrategy - LP做市策略
├── 账户管理器 (AccountManager)
│   ├── 独立的私钥和钱包管理
│   ├── 独立的风险控制
│   └── 策略分配管理
├── 执行引擎 (ExecutionEngine)
│   ├── 策略调度和执行
│   ├── 账户隔离
│   └── 并发控制
└── 命令行接口 (CLI)
    ├── 实时配置管理
    ├── 状态监控
    └── 交互式控制
```

## 🚀 快速开始

### 1. 环境配置

确保 `.env` 文件包含必要配置：

```bash
# API配置
API_BASE_URL=https://api.limitless.exchange
RPC_URL=https://mainnet.base.org

# 账户私钥
AUTH_PRIVATE_KEY=your_private_key_here
AUTH_PRIVATE_KEY_2=your_second_private_key_here
AUTH_PRIVATE_KEY_3=your_third_private_key_here

# 市场发现配置
MARKET_SCAN_INTERVAL=30
MARKET_DISCOVERY_INTERVAL=300
MIN_MARKET_SCORE=60
```

### 2. 启动系统

#### 方式1: 交互式CLI (推荐)
```bash
npm run multi-strategy
```

启动后使用以下命令：
- `start` - 启动交易引擎
- `accounts` - 查看账户状态
- `strategies` - 查看策略状态
- `help` - 查看所有命令

#### 方式2: 运行演示
```bash
npm run demo:multi
```

#### 方式3: 运行测试
```bash
npm run test:multi
```

## 📋 系统配置

### 默认配置示例

系统默认配置实现了您的需求：

```javascript
{
    accounts: [
        {
            id: 'account1',
            name: '账户1',
            strategies: ['NewMarketSplit', 'LPMaking'], // 执行策略A+B
            maxRisk: 1000,
            strategyConfigs: [
                {
                    type: 'NewMarketSplit',
                    config: {
                        splitAmount: 100,        // 100u split
                        minMarketScore: 70,
                        maxConcurrentSplits: 3
                    }
                },
                {
                    type: 'LPMaking', 
                    config: {
                        initialPurchase: 50,     // 购买50份额
                        targetProfitRate: 0.15,  // 15%止盈率
                        maxConcurrentMarkets: 5
                    }
                }
            ]
        },
        {
            id: 'account2',
            name: '账户2',
            strategies: ['NewMarketSplit'],              // 仅执行策略A
            maxRisk: 500
        },
        {
            id: 'account3', 
            name: '账户3',
            strategies: ['LPMaking'],                    // 仅执行策略B
            maxRisk: 800
        }
    ]
}
```

## 🎯 策略详解

### 策略A: 新市场Split策略

**功能**: 实时监控市场，当发现新市场时(且没有正在进行的split市场策略)，将100u split成yes和no

**特点**:
- ✅ 基于市场发现服务实时监控新市场
- ✅ 智能评分筛选优质市场
- ✅ 冲突检测：避免与其他split策略冲突
- ✅ 冷却期机制：防止过于频繁操作
- ✅ 并发限制：控制同时进行的split数量

**配置参数**:
```javascript
{
    splitAmount: 100,           // Split金额 (USDC)
    minMarketScore: 70,         // 最小市场评分阈值
    maxConcurrentSplits: 3,     // 最大并发Split数量
    cooldownPeriod: 300000      // 冷却期 (5分钟)
}
```

### 策略B: LP做市策略

**功能**: 选择一个合适市场的合适方向，购买50份额，然后去做LP，设置止盈率

**特点**:
- ✅ 智能市场选择：基于评分算法选择最优市场
- ✅ 初始购买：购买指定份额建立仓位
- ✅ LP做市：设置限价订单进行做市
- ✅ 动态价格调整：尽可能不成交但获得LP奖励
- ✅ 止盈机制：达到止盈率时允许订单成交

**配置参数**:
```javascript
{
    initialPurchase: 50,        // 初始购买份额
    targetProfitRate: 0.15,     // 目标止盈率 (15%)
    maxConcurrentMarkets: 5,    // 最大并发市场数
    priceAdjustmentInterval: 300000  // 价格调整间隔 (5分钟)
}
```

## 🖥️ 命令行界面

### 主要命令

```bash
# 引擎控制
start                    # 启动交易引擎
stop                     # 停止交易引擎
status                   # 显示引擎状态

# 账户管理
accounts                 # 显示所有账户
add-account              # 添加新账户
activate-account <id>    # 激活账户
deactivate-account <id>  # 停用账户

# 策略管理
strategies               # 显示策略状态
stats                    # 显示执行统计

# 其他
help                     # 显示帮助
clear                    # 清屏
exit                     # 退出系统
```

### 使用示例

```bash
🤖 MultiStrategy> start
🚀 启动交易引擎...
✅ 交易引擎启动成功

🤖 MultiStrategy> accounts
👤 账户信息:
account1 (账户1)
  地址: 0x1234...
  状态: 🟢 活跃
  策略: NewMarketSplit, LPMaking
  实例: 2 个

🤖 MultiStrategy> strategies
🎯 策略状态:
账户: account1 (账户1)
  📊 NewMarketSplitStrategy:
     运行状态: 🟢 运行中
     活跃Split: 0
     已处理市场: 5
  📊 LPMakingStrategy:
     运行状态: 🟢 运行中
     活跃仓位: 2
     限价订单: 2
```

## 📊 监控和事件

### 实时事件通知

系统会实时显示策略执行事件：

```bash
📢 账户策略事件 [账户1/NewMarketSplitStrategy]: splitCompleted
   ✅ Split完成: 市场 Presidential Election 2024...
   
📢 账户策略事件 [账户3/LPMakingStrategy]: purchaseCompleted
   💰 购买完成: BUY 50 @ 0.6234
   
📢 账户策略事件 [账户1/LPMakingStrategy]: orderAdjustedForProfit
   🎯 订单调整止盈: 新价格 0.7180
```

### 状态监控

```bash
📊 系统状态:
引擎状态: 🟢 运行中
总账户数: 3
活跃账户: 3
总策略数: 4
执行次数: 25
成功次数: 23
失败次数: 2
```

## 🛡️ 安全特性

### 1. 交易安全
- ✅ 所有真实交易都需要明确确认参数
- ✅ 内置安全检查：`confirmRealTransaction=true`
- ✅ 测试模式：可以创建订单数据但不执行交易
- ✅ 风险控制：每个账户独立的风险限制

### 2. 账户隔离
- ✅ 独立的私钥管理
- ✅ 独立的风险控制
- ✅ 独立的策略配置
- ✅ 独立的执行状态

### 3. 系统稳定性
- ✅ 优雅启动和停止
- ✅ 错误隔离：单个策略错误不影响其他策略
- ✅ 状态恢复：系统重启后可恢复状态
- ✅ 并发控制：防止资源竞争

## 📁 文件结构

```
src/
├── core/                       # 核心组件
│   ├── execution-engine.js     # 执行引擎
│   ├── api-client.js           # API客户端
│   └── config.js               # 配置管理
├── strategies/                 # 策略组件
│   └── multi-strategy-system.js # 策略实现
├── managers/                   # 管理器组件
│   └── account-manager.js      # 账户管理
├── interfaces/                 # 界面组件
│   └── cli-interface.js        # 命令行界面
└── multi-strategy-main.js      # 主入口

examples/
├── config-examples.js          # 配置示例
└── demo-multi-strategy.js      # 演示脚本

tests/
└── test-multi-strategy.js      # 测试脚本

docs/
└── multi-strategy-guide.md     # 详细使用指南
```

## 🔧 自定义配置

### 保守策略配置
```javascript
{
    splitAmount: 50,            // 较小金额
    minMarketScore: 80,         // 高评分要求
    maxConcurrentSplits: 1,     // 单一操作
    targetProfitRate: 0.10      // 较低止盈率
}
```

### 激进策略配置
```javascript
{
    splitAmount: 150,           // 较大金额
    minMarketScore: 60,         // 较低评分要求
    maxConcurrentSplits: 5,     // 多重操作
    targetProfitRate: 0.25      // 较高止盈率
}
```

## 🧪 测试和验证

### 运行测试
```bash
npm run test:multi
```

### 运行演示
```bash
npm run demo:multi
```

### 测试覆盖
- ✅ 配置验证测试
- ✅ 账户管理测试
- ✅ 策略创建测试
- ✅ 执行引擎测试
- ✅ 集成测试

## 📈 性能特性

- **并发执行**: 支持多账户多策略并发运行
- **资源优化**: 智能资源分配和负载均衡
- **缓存机制**: API响应缓存和数据优化
- **异步处理**: 非阻塞异步操作架构

## 🔄 扩展性

系统设计支持：
- ✅ 添加新的交易策略
- ✅ 扩展账户管理功能
- ✅ 集成外部监控系统
- ✅ 自定义事件处理器

## 📞 使用支持

### 快速开始
1. 配置环境变量
2. 运行 `npm run multi-strategy`
3. 使用 `start` 命令启动系统
4. 使用 `help` 查看所有可用命令

### 故障排除
- 检查 `.env` 配置
- 查看系统日志输出
- 使用 `status` 命令检查状态
- 运行测试验证功能

### 文档参考
- `docs/multi-strategy-guide.md` - 详细使用指南
- `examples/config-examples.js` - 配置示例
- `examples/demo-multi-strategy.js` - 演示代码

---

## 🎉 总结

这个多策略多账户交易执行系统完全实现了您的需求：

1. **策略A**: ✅ 新市场Split策略，实时监控，智能筛选，100u split
2. **策略B**: ✅ LP做市策略，购买50份额，动态调价，止盈控制
3. **多账户**: ✅ 账户1执行A+B，账户2执行A，账户3执行B
4. **命令行**: ✅ 灵活的配置管理和实时监控界面

系统具备生产级的安全性、稳定性和可扩展性，可以立即投入使用！